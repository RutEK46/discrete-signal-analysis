\chapter{Prezentacja aplikacji}
\section{Wykorzystywane narzêdzia i biblioteki}
\subsection{Pycharm for Anaconda}
\begin{figure*}[!h]
  % wyœrodkowanie zawartoœci pola obrazka
  \begin{center}
    % okienko skaluj¹ce:
    %  pierwszy argument szerokoœæ, drugi wysokoœæ,
    %  jeden z nich mo¿e byæ zast¹piony ! - zachowanie proporcji obrazka
    %  w taki sposób mo¿emy skalowaæ tak¿e inne obiekty np. tekst
    \resizebox{1\textwidth}{!}{
      % wstawienie obrazka
      \includegraphics{rysunki/PyCharm_for_Anaconda}
    }
    % opis obrazka
    \caption[PyCharm for Anaconda]{Grafika ze strony producenta.}
    % etykieta
    \label{PyCharm for Anaconda}
  \end{center}
\end{figure*}
PyCharm jest IDE stworzonym dla deweloperów Python. Pozwala na analizowanie kodu Pythona na bierz¹co w celu pomagania programiœcie w pisaniu kodu oraz, je¿eli jest w stanie to wykryæ, informuje go o potencjalnych b³êdach, które móg³ pope³niæ. Jest dostêpne w wersji Professional oraz Community, z których druga jest darmowa.
\subsection{Jupyter Notebook}
Jupyter Notebook jest narzêdziem dostarczaj¹cym swój w³asny format plików pozwalaj¹ce na tworzenie tych plików, ich edycjê oraz uruchamianie w œrodowisku internetowym. Jupyter Notebook jest czêsto stosowany do prototypowania kodu napisanego w Python oraz testowania tego kodu oraz jako oddzielne GUI dla aplikacji. Plik posiada strukturê komórkow¹, gdzie ka¿da komórka zawiera kod napisany w Python, który mo¿na uruchomiæ i zobaczyæ wyniki. Komórki po uruchomieniu zapamiêtuj¹ u¿ywane zmienne, przez co nastêpne komórki mog¹ je wykorzystaæ. W ten sposób mo¿na ³atwo napisaæ prototyp kodu z dziel¹c poszczególne komponenty na komórki, a nastêpnie testowaæ pojedyncz¹ komórkê bez koniecznoœci uruchamiania ca³ego kodu.
\subsection{Numpy}
Numpy jest bibliotek¹ do obliczeñ numerycznych zawieraj¹c¹ wiele funkcjonalnoœci znanych ze œrodowiska Matlab. Pozwala na wygodne tworzenie wielowymiarowych macierzy wraz z mo¿liwoœcia wykonywania operacji na nich.
\subsection{Numba}
\begin{figure*}[!h]
  % wyœrodkowanie zawartoœci pola obrazka
  \begin{center}
    % okienko skaluj¹ce:
    %  pierwszy argument szerokoœæ, drugi wysokoœæ,
    %  jeden z nich mo¿e byæ zast¹piony ! - zachowanie proporcji obrazka
    %  w taki sposób mo¿emy skalowaæ tak¿e inne obiekty np. tekst
    \resizebox{1\textwidth}{!}{
      % wstawienie obrazka
      \includegraphics{rysunki/Numba}
    }
    % opis obrazka
    \caption[Numba]{Grafika ze strony producenta.}
    % etykieta
    \label{Numba}
  \end{center}
\end{figure*}
Numba jest bibliotek¹ dostêpn¹ w œrodowisku Anaconda pozwalaj¹c¹ na przyspieszenie kodu napisanego w Python porzez kompilatory JIT (Just-In-Time). Nie wspiera wszystkich funkcjonalnoœci jêzyka Python, zato w swoich funkcjonalnoœciach szczególnie uwglêdnia bibliotekê Numpy. Numba posiada te¿ specjalny JIT dla cuda (dla kart graficnych Nvidii) pozwalaj¹cy na skompilowanie kodu napisanego w Python kod wykonywalny na karcie graficznej.
\subsection{Cudatoolkit}
Cudatoolkit jest potrzebny do uruchomienia kompilatora JIT dla cudy w bibliotece Numba. Dostarcza tej bibliotece mo¿liwoœæ komunikowania siê z kart¹ graficzn¹. Z poziomu programisty jest nie wymaga nic poza zainstalowaniem przez œrodowisko Anacondy.
\subsection{Pandas}
Pandas jest bibliotek¹ dostarczaj¹c¹ struktury danych pozwalaj¹c¹ w wygodny sposób operowaæ na danych. Jej g³ówn¹ zalet¹ jest obs³uga plików z wartoœciami oddzielonymi przecinkami, czyli plików csv (comma-separeted values).
\subsection{Plotly}
Plotly jest bibliotek¹ pozwalaj¹c¹ na tworzenie wykresów w aplikacjach w œrodowisku internetowym (takim jak Jupyter).
\section{Instrukcja}
\subsection{Wymagania sprzêtowe}
\subsubsection{Minimalne wymagania sprzêtowe producenta modu³u cuda w bibliotece Numba:}
\begin{itemize}
\item karta graficzna: NVIDIA z mo¿liwoœciami obliczeniowymi 2.0 lub wiêksze
\end{itemize}

\vspace{0.3cm}
\subsubsection{Konfiguracja sprzêtowa, na której by³y wykonywane testy:}
\begin{itemize}
\item system operacyjny: Windows 8.1
\item procesor: Intel Core i5-4200H
\item 8 GB RAM
\item karta graficzna: NVIDIA GeForce GTX 860M
\end{itemize}

\subsection{Instalacja i konfiguracja}
\subsubsection{Instalacja}
Dla prawid³owego dzia³ania aplikacji nale¿y zainstalowaæ œrodowisko Anaconda, jeœli nie ma go na danym urz¹dzeniu. W tym celu nale¿y:
\begin{itemize}
\item Instalator mo¿na pobraæ na przyk³ad ze strony Anaconda.
\item Nastepnie nale¿y wybraæ odpowiedni¹ wersjê instalatora odpowiedni¹ dla systemu operacyjnego na u¿ywanym komputerze.
\item Nale¿y pobraæ wersjê Python 3.5 lub nowsz¹.
\end{itemize}
\vspace{0.3cm}

\subsubsection{Konfiguracja}
Po zainstalowaniu œrodowiska Anaconda, nale¿y skonfigurowaæ œrodowisko Anaconda za pomoc¹ narzêdzia ''Anaconda Prompt'' dostarczanego w instalatorze Anaconda:
\begin{itemize}
\item Nale¿y najpierw zaktualizowaæ narzêdzie conda za pomoc¹ polecenia: \\ ''conda update conda''.
\item Nale¿y najpierw zaktualizowaæ narzêdzie jupyter za pomoc¹ polecenia: \\ ''conda update jupyter''.
\item Nastepnie nale¿y zainstalowaæ wymagane biblioteki. Miêdzy innymi numpy, pandas, \\ matplotlib za pomoc¹ polecenia przyk³adowo ''conda install numpy''.
\item Nale¿y tak¿e zainstalowaæ cudatoolkit. W tym celu nale¿y u¿yæ polecenia \\ ''conda install -c anaconda cudatoolkit''.
\end{itemize}

\subsection{Instrukcja obs³ugi}
\subsubsection{Uruchomienie}
Gdy œrodowisko bêdzie ju¿ przygotowane, nastêpnym krokiem bêdzie uruchomienie narzêdzia jupyter notebook. W tym celu nale¿y w narzêdziu ''Anaconda Prompt'' wpisaæ polecenie ''jupyter notebook''. Efektem jest uruchomienie notatnika w domyœlnej przegl¹darce internetowej.

Po uruchomieniu œrodowiska zostaje wyœwietlony ekran g³ówny notatnika jupyter. Nale¿y przejœæ do lokalizacji katalogu, w którym znajduje siê projekt. Nastêpnie nale¿y wybraæ plik ''main.ipynb''. Efektem tego dzia³ania powinno byæ uruchomienie aplikacji w drugiej karcie przegl¹darki. Domyœlnie aplikacja jest otwierana na pierwszej karcie aplikacji, czyli na interfejsie standardowym.
\begin{figure*}[!h]
  % wyœrodkowanie zawartoœci pola obrazka
  \begin{center}
    % okienko skaluj¹ce:
    %  pierwszy argument szerokoœæ, drugi wysokoœæ,
    %  jeden z nich mo¿e byæ zast¹piony ! - zachowanie proporcji obrazka
    %  w taki sposób mo¿emy skalowaæ tak¿e inne obiekty np. tekst
    \resizebox{1\textwidth}{!}{
      % wstawienie obrazka
      \includegraphics{rysunki/menu_jupyter}
    }
    % opis obrazka
    \caption[Menu jupyter]{Ekran notatnika jupyter z katalogiem projektu, w którym znajduje siê plik uruchomieniowy main.ipynb.}
    % etykieta
    \label{Menu jupyter}
  \end{center}
\end{figure*}

\subsubsection{Interfejs standardowy}

to do

\subsubsection{Interfejs inwestora}

to do

\subsubsection{Inerfejs zagragowanego wskaŸnika}

to do



